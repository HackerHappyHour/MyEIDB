# Migration `watch-20191124172941`

This migration has been generated by Brian vanderbusch at 11/24/2019, 5:29:41 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "initial_myeidb"."User" (
  "email" text NOT NULL DEFAULT '' ,
  "id" text NOT NULL  ,
  "name" text   ,
  PRIMARY KEY ("id")
);

CREATE TABLE "initial_myeidb"."Component" (
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text NOT NULL  ,
  "name" text NOT NULL DEFAULT '' ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "User.id" ON "initial_myeidb"."User"("id")

CREATE UNIQUE INDEX "User.email" ON "initial_myeidb"."User"("email")

CREATE UNIQUE INDEX "Component.id" ON "initial_myeidb"."Component"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20191124172941
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,21 @@
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://myeidb_admin:password@localhost/myeidb?schema=initial_myeidb"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model User {
+  id    String  @default(cuid()) @id @unique
+  email String  @unique
+  name  String?
+}
+
+model Component {
+  id        String   @default(cuid()) @id @unique
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  name     String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191124172941)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191124172941'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
